# Neon Postgres (pooled connection — required)
DATABASE_URL="postgresql://..."

# Vercel AI Gateway OIDC (auto-provisioned by `vercel env pull`)
VERCEL_OIDC_TOKEN="..."

# Brave Search API key (for newsdesk web research)
BRAVE_API_KEY="..."

# Google Generative AI (for embeddings — gemini-embedding-exp-03-07)
GOOGLE_GENERATIVE_AI_API_KEY="..."

# Vercel Blob (for AI-generated article images)
BLOB_READ_WRITE_TOKEN="..."

# Telegram Bot API (for publish/audit notifications to Lee)
TELEGRAM_BOT_TOKEN="..."

# Vercel Cron secret (auto-set on Vercel, needed locally for testing trigger/audit endpoints)
CRON_SECRET="..."
